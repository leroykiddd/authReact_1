export const enum LocalStorageKeys {
  TOKEN = 'token',
  USER_ID = 'user_id',
}

export const enum ApiUrlEnum {
  INFO = '/info',
  LOGIN = '/auth/login',
  USER_INIT = '/users/',
  CONTACTS = '/users',
}
